// Placeholder app for the template. Each exercise instance replaces the
// contents of src/ with that session's starter code.

function App() {
    return (
        <div>
            <h1>React exercise</h1>
            <p>Replace this with the session starter.</p>
        </div>
    );
}

export default App;
